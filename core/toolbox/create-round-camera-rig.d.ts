import babylon from "../../babylon";
import { RoundCameraRigOptions } from "./toolbox-interfaces";
export declare const createRoundCameraRig: ({ scene, canvas, targetPosition, radius, active }: RoundCameraRigOptions) => babylon.ArcRotateCamera;
